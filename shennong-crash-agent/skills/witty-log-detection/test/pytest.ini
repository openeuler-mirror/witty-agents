[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    async: async test
    slow: slow test
    integration: integration test
    unit: unit test
addopts = -v --tb=short
testpaths = 
    test_worker.py
    test_client.py
